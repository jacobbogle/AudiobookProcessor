
[pytest]
addopts = -q
log_cli = true
log_cli_level = INFO

# Show local variables on failures (makes failures easier to inspect)
# Note: requires pytest>=3.3; if unavailable this line will be ignored
xfail_strict = false

# Custom markers
markers =
	integration: marks integration tests that require ffmpeg/ffprobe
